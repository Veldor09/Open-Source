services:
  postgres:
    image: postgres:16-alpine
    container_name: observatorio-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-observatorio}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-observatorio}
      POSTGRES_DB: ${POSTGRES_DB:-observatorio_cr}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-observatorio} -d ${POSTGRES_DB:-observatorio_cr}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
